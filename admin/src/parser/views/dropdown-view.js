require('../../utils/array')

const component = {
  name: 'dropdown-view',
  template: `<dropdown-view>{{content}}</dropdown-view>`,
  parser: function (metadata, store) {}
}

module.exports = component
